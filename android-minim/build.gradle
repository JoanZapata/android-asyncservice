apply plugin: 'android-library'

dependencies {
    compile 'com.snappydb:snappydb-api:0.2.+'
    compile project(':android-minim-processors')
}

android {

    defaultConfig {
        minSdkVersion 9
        targetSdkVersion 19
    }

    compileSdkVersion 19
    buildToolsVersion "19.0.3"
}